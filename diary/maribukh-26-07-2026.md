2026-07-17

What I worked on

Started the Hooks and Routing task. Created the hooks-and-routing branch from unit-testing, installed react-router-dom.

What I did
Converted the simple components first, Loader, Header, ErrorMessage, CardListSkeleton, BuggyButton, to functional components with hooks. Extracted useSearchTerm as a custom hook for the localStorage logic, per the requirement to keep that logic in a dedicated hook.

Where I got stuck
Wasn't sure whether ErrorBoundary really had to stay a class, double checked the task, confirmed it's an explicit exception.

How I worked through it

Left ErrorBoundary untouched as a class component, converted everything else.

Decisions made
localStorage access goes through a single custom hook (useSearchTerm), not scattered across components. ErrorBoundary stays a class, per the task exception.

Open questions
How to structure pagination and the master-detail view together without the two features fighting over the same state.

Time spent
4h
