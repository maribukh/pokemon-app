2026-07-20

What I worked on

Finished tests for HomePage, pokemonApi, App, ErrorBoundary, closed out the remaining coverage.

What I did

Mocked fetch in pokemonApi.test.ts using vi.stubGlobal. Mocked the whole pokemonApi module in HomePage.test.tsx using vi.mock, covered a successful list, exact search, API error, and searching for a pokemon that doesn't exist. Filled the last coverage gap in ErrorBoundary (was missing a test for the Reload button/window.location.reload). Got overall statement coverage to 99%.

Where I got stuck

App.tsx kept showing 0% coverage. Turned out I just forgot to actually save the App.test.tsx file, even though I'd already written the code and thought it was done.
How I worked through it
Ran ls src/App* and saw the file wasn't there. Recreated it, ran the tests again, coverage picked up right away.

Decisions made

Mocking the API at the module level (vi.mock) rather than at the fetch level for component tests, feels cleaner and closer to testing behavior instead of implementation. Kept the same overall mocking approach consistent between pokemonApi.test.ts (low level, fetch) and HomePage.test.tsx (high level, whole module).

Open questions

None left. Next week is converting classes to functional components, tests are already written to survive that.

Time spent
4h
