## 2026-07-30

### What I worked on

- Started the **State Management** task.
- Created the `app-state-management` branch.
- Installed **Zustand**.

### What I did

- Created a Zustand store:
  - `selectedItems`
  - `toggleItem`
  - `unselectAll`
- Added a checkbox to the `Card` component.
- Connected `Card` directly to the store to avoid prop drilling.

### Where I got stuck

- Clicking the checkbox also triggered the card's `onClick`, opening the details panel.

### How I worked through it

- Used `stopPropagation()` in the checkbox `onClick` and `onChange` handlers.

### Decisions made

- Chose **Zustand** instead of Redux Toolkit because it requires less setup.
- Let `Card` subscribe directly to the store instead of passing props.

### Open questions

- Should the Flyout use `position: sticky` or `position: fixed`?

### Time spent

**3h**
