2026-07-20

What I worked on

Built Pagination, PokemonDetails, About, and NotFound pages, wired up routing in App.tsx.
Fixed failing tests after the router refactor and addressed code review comments.

What I did

Added a Pagination component synced with the page query param. Built PokemonDetails using useParams/useSearchParams, rendered through <Outlet> inside HomePage. Added About and NotFound pages, linked them from Header using NavLink so the active route is visually marked.

Wrapped HomePage and Header tests in MemoryRouter, since useSearchParams/useLocation/NavLink need router context, this doesn't change any assertions, just the render setup. Fixed two review comments: replaced a querySelectorAll DOM lookup with data-testid + getByTestId in the skeleton test, and mocked utils/storage instead of touching real localStorage directly in Search tests, for proper test isolation. Used test.each where the same test ran with different inputs.

Where I got stuck

ESLint flagged calling setState synchronously inside a data-fetching useEffect (react-hooks/set-state-in-effect), even though that's the required pattern here since no data-fetching library is allowed.

Coverage dropped to 75% after adding new pages and hooks, since none of the new routing pieces (About, NotFound, Pagination, PokemonDetails, usePokemonDetails) had tests yet.

Decisions made
Split PokemonDetails into smaller pieces (DetailsTypes, DetailsStats) instead of one big component, same decomposition style as HomePage. Moved shared logic (image fallback, stat lookup, card mapping) into small utils instead of duplicating it between HomePage and PokemonDetails.

Coverage isn't a scored feature in this task, but it's still a penalty threshold, so treated it the same way as before, kept it above 80%.

Open questions
None left.

Time spent
4h
